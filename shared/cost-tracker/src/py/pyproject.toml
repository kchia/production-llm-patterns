[project]
name = "cost-tracker"
version = "0.1.0"
description = "Shared cost computation and accumulation utility for LLM API spend tracking"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = ["--import-mode=importlib"]

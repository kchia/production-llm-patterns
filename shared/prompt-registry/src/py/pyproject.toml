[project]
name = "prompt-registry"
version = "0.1.0"
description = "Shared versioned prompt registry with alias management and caching"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = ["--import-mode=importlib"]

[project]
name = "regression-testing"
version = "0.1.0"
description = "CI gate that compares LLM outputs before/after prompt changes across tagged query categories"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24"]

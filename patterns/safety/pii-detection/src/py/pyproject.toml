[project]
name = "pii-detection"
version = "1.0.0"
description = "PII detection and redaction pattern for LLM systems"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
import_mode = "importlib"

[tool.mypy]
strict = true

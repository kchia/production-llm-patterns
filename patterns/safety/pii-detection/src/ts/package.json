{
  "name": "pii-detection",
  "version": "1.0.0",
  "description": "PII detection and redaction pattern for LLM systems — hybrid regex + NER detection with reversible redaction",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.2",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}

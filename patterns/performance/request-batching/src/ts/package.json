{
  "name": "request-batching",
  "version": "1.0.0",
  "description": "Request Batching pattern — group LLM API calls for throughput efficiency",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "npx tsx ../benchmarks/bench.ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.6.0"
  }
}

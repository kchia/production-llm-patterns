{
  "name": "token-budget-middleware",
  "version": "1.0.0",
  "description": "Token budget enforcement middleware for LLM systems — per-request and cumulative cost control",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}

{
  "name": "structured-tracing",
  "version": "1.0.0",
  "description": "Structured tracing for LLM pipelines — framework-agnostic, with automatic span nesting and LLM-specific attributes",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
